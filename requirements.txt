# Core Jupyter dependencies
jupyter>=1.0.0
jupyterlab>=4.2.5
notebook>=7.2.2

# Scientific computing
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.10.0

# Plotting and visualization
plotly>=5.14.0
ipywidgets>=8.0.0

# Mass spectrometry
# NOTE: the notebooks pin a pyOpenMS *nightly* build that is vendored in wheels/.
# To match the course environment exactly on Linux x86_64, install it instead:
#   pip install wheels/pyopenms-*-cp312-cp312-manylinux_2_34_x86_64.whl
pyopenms>=3.5.0
pyopenms-viz>=1.0.0
